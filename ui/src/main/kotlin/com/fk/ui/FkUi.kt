package com.fk.ui

/**
 * Entry marker for the UI library.
 *
 * Maps conceptually to iOS `FKUIKit`: design tokens and high-value
 * Compose components (not 1:1 ports of every Material control).
 */
object FkUi {
  /** Library semantic version (keep in sync with `FK_VERSION_NAME`). */
  const val VERSION: String = "0.1.0"
}
