package com.fk.core

/**
 * Entry marker for the foundation library.
 *
 * Maps conceptually to iOS `FKCoreKit`: networking, storage, security,
 * pluggable contracts, and other non-UI infrastructure.
 */
object FkCore {
    /** Library semantic version (keep in sync with `FK_VERSION_NAME`). */
    const val VERSION: String = "0.1.0"
}
