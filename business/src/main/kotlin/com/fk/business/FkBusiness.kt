package com.fk.business

/**
 * Entry marker for the business UI library.
 *
 * Maps conceptually to iOS `FKBusinessKit`: comment kit, filter strip,
 * and selective business list-row patterns — Compose implementations,
 * not Activity/Fragment base-class shells.
 */
object FkBusiness {
    /** Library semantic version (keep in sync with `FK_VERSION_NAME`). */
    const val VERSION: String = "0.1.0"
}
